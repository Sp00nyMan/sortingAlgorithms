#pragma once
const size_t WIDTH = 900;
const size_t HEIGHT = 900;

const sf::Color bgColor = sf::Color(220,220,220);
const float textScale = 5.0f; //The bigger the value - the smaller is the text 

const sf::Color buttonTextColor = bgColor;
const sf::Color buttonColor = sf::Color(74, 134, 204);

const sf::Color buttonTextColorOnHover = buttonColor;//sf::Color(0, 255, 255);
const sf::Color buttonColorOnHover = sf::Color(0, 255, 235);

const std::string defaultFont = "resources/font.ttf";